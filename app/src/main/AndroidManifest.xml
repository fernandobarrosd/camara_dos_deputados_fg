<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CamaraDosDeputadosFG">

        <activity android:name=".ui.authenticated.activities.UpdatePasswordActivity"
            android:exported="false"/>

        <activity android:name=".ui.notAuthenticated.activities.LoginActivity"
            android:exported="false"/>

        <activity android:name=".ui.notAuthenticated.activities.RegisterActivity"
            android:exported="false"/>

        <activity android:name=".ui.notAuthenticated.activities.WelcomeActivity"
            android:exported="false"/>
        <activity android:name=".ui.authenticated.activities.PrivateActivity"
            android:exported="false"/>
        <activity
            android:name=".ui.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>